#ifndef VAULTCONTROLLER_H
#define VAULTCONTROLLER_H

#include <QObject>
#include <QDir>
#include <QDebug>
#include "logger.h"

class VaultController : public QObject {
    Q_OBJECT
public:
//    explicit VaultController(QObject *parent = nullptr);

//    Q_INVOKABLE bool createVaultFolder(const QString &vaultName, const QString &password);
//    Q_INVOKABLE bool copyPathToVault(QString sourcePath, QString vaultName, QString password);

private:
//     bool copyRecursively(const QString &srcFilePath, const QString &targetFilePath, int parentId);
};

#endif // VAULTCONTROLLER_H
